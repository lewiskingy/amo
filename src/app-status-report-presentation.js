/* Status Report presentation is owned by app-report-renderer.js.
   This file remains as a compatibility asset for existing deployment manifests, but intentionally
   does not wrap reportNarrativeHtml(), dashboardSnapshotHtml() or renderStatusReporting(). */
(function initStatusReportPresentation(){
  if(window.__amoStatusReportPresentationLoaded)return;window.__amoStatusReportPresentationLoaded=true;
})();
