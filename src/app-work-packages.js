/* Canonical Work Package domain module.
   Work Packages are first-class child records of Defined Demand. Allocations and Actuals remain at Demand level. */
(function initWorkPackages(){
  const ENTITY_TYPE='workPackages',LOCAL_FOLDER='work-packages';
  const state={rows:[],loaded:false,loading:false,editingId:null};
  const esc=v=>typeof escHtml==='function'?escHtml(String(v??'')):String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
  const trim=v=>String(v??'').trim();
  const configuredDepartmentsSafe=()=>typeof configuredDepartments==='function'?configuredDepartments():(db.settings?.departments||[]);
  const configuredTeamsSafe=()=>typeof configuredTeams==='function'?configuredTeams():(db.settings?.teams||[]);
  const teamByIdSafe=id=>configuredTeamsSafe().find(t=>t.id===id)||null;
  const departmentByIdSafe=id=>configuredDepartmentsSafe().find(d=>d.id===id)||null;

  function azureDevOpsContextForDemand(demand){
    const team=teamByIdSafe(demand?.teamId),department=departmentByIdSafe(team?.departmentId||demand?.departmentId)||configuredDepartmentsSafe()[0]||null;
    const system=db.settings?.azureDevOps||{},departmentConfig=department?.azureDevOps||{},teamConfig=team?.azureDevOps||{};
    const organization=trim(teamConfig.organization)||trim(departmentConfig.organization)||trim(system.defaultOrganization);
    const project=trim(teamConfig.project)||trim(departmentConfig.project);
    return{organization,project,team,department,organizationSource:trim(teamConfig.organization)?'Team':trim(departmentConfig.organization)?'Department':trim(system.defaultOrganization)?'System':'Not configured',projectSource:trim(teamConfig.project)?'Team':trim(departmentConfig.project)?'Department':'Not configured'}
  }
  function workItemUrl(workPackage,demand){
    const id=trim(workPackage?.azureDevOpsWorkItemId),ctx=azureDevOpsContextForDemand(demand);if(!id||!ctx.organization||!ctx.project)return'';
    return`https://dev.azure.com/${encodeURIComponent(ctx.organization)}/${encodeURIComponent(ctx.project)}/_workitems/edit/${encodeURIComponent(id)}`
  }
  function nextId(){const max=Math.max(0,...state.rows.map(x=>Number(String(x.id||'').match(/(\d+)$/)?.[1]||0)));return`WP-${String(max+1).padStart(4,'0')}`}
  function normalize(record){return{id:trim(record?.id),demandId:trim(record?.demandId),title:trim(record?.title),description:trim(record?.description),acceptanceCriteria:trim(record?.acceptanceCriteria),status:trim(record?.status)||'Planned',romDays:record?.romDays==null||record?.romDays===''?null:Number(record.romDays),targetStart:trim(record?.targetStart),targetEnd:trim(record?.targetEnd),azureDevOpsWorkItemId:trim(record?.azureDevOpsWorkItemId),version:Number(record?.version)||1,modifiedAt:record?.modifiedAt||null}}
  function validate(record){if(!record.demandId)throw new Error('Demand is required.');if(!db.demand?.some(d=>d.id===record.demandId))throw new Error('The selected Demand no longer exists.');if(!record.title)throw new Error('Work Package title is required.');if(record.romDays!=null&&(!Number.isFinite(record.romDays)||record.romDays<0))throw new Error('ROM Days must be zero or greater.');if(record.azureDevOpsWorkItemId&&!/^\d+$/.test(record.azureDevOpsWorkItemId))throw new Error('Azure DevOps Work Item Reference must be a numeric work item ID.');return record}

  async function listRecords(){const repo=window.workspaceRepository;if(!repo)return[];if(repo.mode==='local')return repo.listJsonRecords(LOCAL_FOLDER,{optional:true});return repo.listRecords(ENTITY_TYPE)}
  async function saveRecord(record){const repo=window.workspaceRepository;if(!repo)throw new Error('Open a workspace first.');if(repo.mode==='local'){if(!await repo.ensureWritePermission())throw new Error('Read/write permission is required.');await repo.writeJson(`${LOCAL_FOLDER}/${record.id}.json`,record);return}await repo.saveRecord(ENTITY_TYPE,record)}
  async function deleteRecord(id){const repo=window.workspaceRepository;if(!repo)throw new Error('Open a workspace first.');if(repo.mode==='local'){if(!await repo.ensureWritePermission())throw new Error('Read/write permission is required.');await repo.deletePath(`${LOCAL_FOLDER}/${id}.json`);return}await repo.deleteRecord(ENTITY_TYPE,id)}
  async function load({force=false}={}){if(!workspaceHandle){state.rows=[];state.loaded=false;return[]}if(state.loading)return state.rows;if(state.loaded&&!force)return state.rows;state.loading=true;try{state.rows=(await listRecords()).map(normalize).filter(x=>x.id&&x.demandId);state.loaded=true;return state.rows}finally{state.loading=false}}
  function forDemand(demandId){return state.rows.filter(x=>x.demandId===demandId).sort((a,b)=>String(a.targetStart||'9999').localeCompare(String(b.targetStart||'9999'))||a.id.localeCompare(b.id))}

  function ensureStyles(){if(document.getElementById('amoWorkPackageStyles'))return;const s=document.createElement('style');s.id='amoWorkPackageStyles';s.textContent=`.wp-card{margin-top:16px}.wp-list{display:grid;gap:9px}.wp-row{border:1px solid var(--line);border-radius:10px;padding:10px 12px;display:grid;grid-template-columns:minmax(0,1fr) auto;gap:12px;align-items:start}.wp-meta{display:flex;gap:6px;flex-wrap:wrap;margin-top:5px}.wp-actions{display:flex;gap:6px}.wp-dialog-backdrop{position:fixed;inset:0;background:rgba(0,0,0,.38);z-index:12000;display:flex;align-items:center;justify-content:center;padding:18px}.wp-dialog{background:var(--panel);color:var(--text);border:1px solid var(--line);border-radius:14px;max-width:720px;width:min(720px,96vw);max-height:92vh;overflow:auto;padding:18px}.wp-form{display:grid;grid-template-columns:1fr 1fr;gap:10px}.wp-form .full{grid-column:1/-1}.wp-form label{display:grid;gap:5px;font-size:.78rem;font-weight:800;color:var(--muted)}.wp-form textarea{min-height:92px}@media(max-width:680px){.wp-form{grid-template-columns:1fr}.wp-form .full{grid-column:auto}.wp-row{grid-template-columns:1fr}}`;document.head.appendChild(s)}

  async function renderDemandPanel(){
    const host=document.getElementById('demandDetail');if(!host||!workspaceHandle)return;await load();const demand=typeof selectedDemandId!=='undefined'?db.demand?.find(d=>d.id===selectedDemandId):null;if(!demand){host.querySelector('#amoWorkPackagesCard')?.remove();return}
    ensureStyles();let card=host.querySelector('#amoWorkPackagesCard');if(!card){card=document.createElement('div');card.id='amoWorkPackagesCard';card.className='card wp-card';host.appendChild(card)}
    const rows=forDemand(demand.id),ctx=azureDevOpsContextForDemand(demand);
    card.innerHTML=`<div class="section-title" style="margin-top:0"><div><h2>Work Packages</h2><p class="muted">Discrete delivery packages beneath this Defined Demand. Azure DevOps links are derived from the Work Item Reference and inherited Team/Department/System context.</p></div><button class="btn primary" id="addWorkPackage">+ Work Package</button></div>${rows.length?`<div class="wp-list">${rows.map(w=>{const url=workItemUrl(w,demand);return`<div class="wp-row"><div><strong>${esc(w.title)}</strong><div class="wp-meta"><span class="pill blue">${esc(w.id)}</span><span class="pill">${esc(w.status||'Planned')}</span>${w.romDays!=null?`<span class="pill">ROM ${esc(w.romDays)}d</span>`:''}${w.azureDevOpsWorkItemId?`<span class="pill">AzDO #${esc(w.azureDevOpsWorkItemId)}</span>`:''}</div>${w.description?`<div class="muted" style="margin-top:7px">${esc(w.description)}</div>`:''}${w.azureDevOpsWorkItemId?(url?`<div style="margin-top:7px"><a href="${esc(url)}" target="_blank" rel="noopener noreferrer">Open Azure DevOps Work Item #${esc(w.azureDevOpsWorkItemId)}</a></div>`:`<div class="notice" style="margin-top:7px">Work Item #${esc(w.azureDevOpsWorkItemId)} is recorded, but its URL cannot be derived until Azure DevOps Organisation and Project are configured.</div>`):''}</div><div class="wp-actions"><button class="btn" data-wp-edit="${esc(w.id)}">Edit</button><button class="btn danger" data-wp-delete="${esc(w.id)}">Delete</button></div></div>`}).join('')}</div>`:'<div class="notice">No Work Packages have been defined for this Demand.</div>'}<div class="muted" style="margin-top:10px">Effective Azure DevOps context: Organisation <strong>${esc(ctx.organization||'Not configured')}</strong> (${esc(ctx.organizationSource)}) · Project <strong>${esc(ctx.project||'Not configured')}</strong> (${esc(ctx.projectSource)}).</div>`;
    card.querySelector('#addWorkPackage')?.addEventListener('click',()=>openEditor({demandId:demand.id}));card.querySelectorAll('[data-wp-edit]').forEach(b=>b.onclick=()=>openEditor(state.rows.find(x=>x.id===b.dataset.wpEdit)));card.querySelectorAll('[data-wp-delete]').forEach(b=>b.onclick=()=>remove(b.dataset.wpDelete))
  }

  function editorHtml(record){const r=normalize(record),demand=db.demand.find(d=>d.id===r.demandId),ctx=azureDevOpsContextForDemand(demand);return`<div class="wp-dialog-backdrop" id="wpDialogBackdrop"><div class="wp-dialog"><div class="section-title" style="margin-top:0"><div><h2>${r.id?'Edit':'New'} Work Package</h2><p class="muted">Child of ${esc(demand?.id||r.demandId)} — ${esc(demand?.title||'Demand')}</p></div></div><div class="wp-form"><label>Work Package ID<input class="cell-input" id="wpId" value="${esc(r.id||nextId())}" readonly></label><label>Status<input class="cell-input" id="wpStatus" value="${esc(r.status||'Planned')}"></label><label class="full">Title<input class="cell-input" id="wpTitle" value="${esc(r.title)}"></label><label class="full">Description / Scope<textarea class="cell-input" id="wpDescription">${esc(r.description)}</textarea></label><label class="full">Acceptance Criteria<textarea class="cell-input" id="wpAcceptance">${esc(r.acceptanceCriteria)}</textarea></label><label>ROM Days<input class="cell-input" id="wpRom" type="number" min="0" step="0.5" value="${r.romDays==null?'':esc(r.romDays)}"></label><label>Azure DevOps Work Item Reference<input class="cell-input" id="wpAzdo" inputmode="numeric" value="${esc(r.azureDevOpsWorkItemId)}" placeholder="e.g. 18742"></label><label>Target Start<input class="cell-input" id="wpStart" type="date" value="${esc(r.targetStart)}"></label><label>Target End<input class="cell-input" id="wpEnd" type="date" value="${esc(r.targetEnd)}"></label></div><div class="notice" style="margin-top:12px">Azure DevOps: ${esc(ctx.organization||'Organisation not configured')} / ${esc(ctx.project||'Project not configured')}. Only the Work Item Reference is persisted on the Work Package.</div><div class="modal-actions" style="margin-top:14px"><button class="btn" id="wpCancel">Cancel</button><button class="btn success" id="wpSave">Save Work Package</button></div></div></div>`}
  function openEditor(record){const demandId=record?.demandId||selectedDemandId;if(!demandId)return;document.getElementById('wpDialogBackdrop')?.remove();document.body.insertAdjacentHTML('beforeend',editorHtml({...record,demandId}));document.getElementById('wpCancel').onclick=()=>document.getElementById('wpDialogBackdrop')?.remove();document.getElementById('wpSave').onclick=()=>persistEditor(demandId,record?.id||null)}
  async function persistEditor(demandId,existingId){const get=id=>document.getElementById(id)?.value??'',existing=existingId?state.rows.find(x=>x.id===existingId):null;const record=validate(normalize({...(existing||{}),id:get('wpId'),demandId,title:get('wpTitle'),description:get('wpDescription'),acceptanceCriteria:get('wpAcceptance'),status:get('wpStatus'),romDays:get('wpRom'),targetStart:get('wpStart'),targetEnd:get('wpEnd'),azureDevOpsWorkItemId:get('wpAzdo'),version:(Number(existing?.version)||0)+1,modifiedAt:new Date().toISOString()}));try{const save=document.getElementById('wpSave');if(save)save.disabled=true;await saveRecord(record);const index=state.rows.findIndex(x=>x.id===record.id);if(index>=0)state.rows[index]=record;else state.rows.push(record);document.getElementById('wpDialogBackdrop')?.remove();log?.(`${existing?'Updated':'Created'} Work Package ${record.id}.`);renderDemandPanel()}catch(e){alert(`Could not save Work Package: ${e.message}`);const save=document.getElementById('wpSave');if(save)save.disabled=false}}
  async function remove(id){const row=state.rows.find(x=>x.id===id);if(!row||!confirm(`Delete Work Package ${id} — ${row.title}?`))return;try{await deleteRecord(id);state.rows=state.rows.filter(x=>x.id!==id);log?.(`Deleted Work Package ${id}.`);renderDemandPanel()}catch(e){alert(`Could not delete Work Package: ${e.message}`)}}

  async function saveAzureSettings(patch){const repo=window.workspaceRepository;if(!repo)throw new Error('Open a workspace first.');const latest=await repo.getSettings();patch(latest);await repo.saveSettings(latest);db.settings={...db.settings,...structuredClone(latest)};db.configFiles=db.configFiles||{};db.configFiles['settings.json']=structuredClone(latest);log?.('Updated Azure DevOps inheritance settings.');decorateConfig();renderDemandPanel()}
  function valueOrInherited(value,inherited){return trim(value)||`Inherited: ${trim(inherited)||'Not configured'}`}
  function systemCard(){const cfg=db.settings?.azureDevOps||{};return`<div class="card" id="amoAzdoSystemCard"><div class="section-title" style="margin-top:0"><div><h2>Azure DevOps</h2><p class="muted config-description">System default used when a Department or Team does not override the Organisation.</p></div></div><div class="settings-field"><label>Default Organisation</label><input class="cell-input" id="amoAzdoSystemOrg" value="${esc(cfg.defaultOrganization||'')}" placeholder="Azure DevOps organisation"><div class="settings-note">Store the organisation name only, not a URL.</div></div><button class="btn success" id="amoAzdoSystemSave" style="margin-top:10px">Save Azure DevOps default</button></div>`}
  function organizationCard(){const systemOrg=trim(db.settings?.azureDevOps?.defaultOrganization),deps=configuredDepartmentsSafe(),teams=configuredTeamsSafe();return`<div class="card settings-card-wide" id="amoAzdoOrganizationCard"><div class="section-title" style="margin-top:0"><div><h2>Azure DevOps inheritance</h2><p class="muted config-description">Department values inherit the System Organisation unless overridden. Teams inherit both Organisation and Project from their Department unless overridden.</p></div></div><div class="table-wrap"><table><thead><tr><th>Scope</th><th>Name</th><th>Organisation override</th><th>Project</th></tr></thead><tbody>${deps.map(d=>`<tr><td>Department</td><td><strong>${esc(d.name)}</strong></td><td><input class="cell-input" data-azdo-dept-org="${esc(d.id)}" value="${esc(d.azureDevOps?.organization||'')}" placeholder="${esc(valueOrInherited('',systemOrg))}"></td><td><input class="cell-input" data-azdo-dept-project="${esc(d.id)}" value="${esc(d.azureDevOps?.project||'')}" placeholder="Default project"></td></tr>${teams.filter(t=>t.departmentId===d.id).map(t=>`<tr><td>Team</td><td>${esc(t.name)}</td><td><input class="cell-input" data-azdo-team-org="${esc(t.id)}" value="${esc(t.azureDevOps?.organization||'')}" placeholder="${esc(valueOrInherited('',trim(d.azureDevOps?.organization)||systemOrg))}"></td><td><input class="cell-input" data-azdo-team-project="${esc(t.id)}" value="${esc(t.azureDevOps?.project||'')}" placeholder="${esc(valueOrInherited('',d.azureDevOps?.project))}"></td></tr>`).join('')}`).join('')}</tbody></table></div><button class="btn success" id="amoAzdoOrganizationSave" style="margin-top:10px">Save Azure DevOps inheritance</button></div>`}
  function decorateConfig(){const content=document.getElementById('configContent');if(!content||!workspaceHandle)return;const active=content.querySelector('[data-settings-tab].active')?.dataset.settingsTab,grid=content.querySelector('.settings-grid');if(!grid)return;if(active==='system'&&!document.getElementById('amoAzdoSystemCard')){grid.insertAdjacentHTML('beforeend',systemCard());document.getElementById('amoAzdoSystemSave').onclick=async()=>{try{const value=trim(document.getElementById('amoAzdoSystemOrg')?.value);await saveAzureSettings(s=>{s.azureDevOps={...(s.azureDevOps||{}),defaultOrganization:value}})}catch(e){alert(`Could not save Azure DevOps settings: ${e.message}`)}}}if(active==='organization'&&!document.getElementById('amoAzdoOrganizationCard')){grid.insertAdjacentHTML('beforeend',organizationCard());document.getElementById('amoAzdoOrganizationSave').onclick=async()=>{try{const deptValues=new Map([...content.querySelectorAll('[data-azdo-dept-org]')].map(el=>[el.dataset.azdoDeptOrg,{organization:trim(el.value),project:trim(content.querySelector(`[data-azdo-dept-project="${CSS.escape(el.dataset.azdoDeptOrg)}"]`)?.value)}])),teamValues=new Map([...content.querySelectorAll('[data-azdo-team-org]')].map(el=>[el.dataset.azdoTeamOrg,{organization:trim(el.value),project:trim(content.querySelector(`[data-azdo-team-project="${CSS.escape(el.dataset.azdoTeamOrg)}"]`)?.value)}]));await saveAzureSettings(s=>{s.departments=(s.departments||[]).map(d=>deptValues.has(d.id)?{...d,azureDevOps:deptValues.get(d.id)}:d);s.teams=(s.teams||[]).map(t=>teamValues.has(t.id)?{...t,azureDevOps:teamValues.get(t.id)}:t)})}catch(e){alert(`Could not save Azure DevOps inheritance: ${e.message}`)}}}}

  const demandHost=document.getElementById('demandDetail');if(demandHost)new MutationObserver(()=>{if(!document.getElementById('amoWorkPackagesCard'))setTimeout(renderDemandPanel,0)}).observe(demandHost,{childList:true,subtree:false});
  const configHost=document.getElementById('configContent');if(configHost)new MutationObserver(()=>setTimeout(decorateConfig,0)).observe(configHost,{childList:true,subtree:true});
  window.addEventListener('amo-workspace-connected',()=>{state.loaded=false;load({force:true}).then(()=>{renderDemandPanel();decorateConfig()}).catch(e=>console.warn('Could not load Work Packages',e))});
  window.addEventListener('amo-workspace-loaded',()=>{state.loaded=false});
  document.querySelector('[data-view="demand"]')?.addEventListener('click',()=>setTimeout(renderDemandPanel,0));
  document.querySelector('[data-view="config"]')?.addEventListener('click',()=>setTimeout(decorateConfig,0));
  window.WorkPackages={state,load,forDemand,normalize,validate,azureDevOpsContextForDemand,workItemUrl,renderDemandPanel};
})();
