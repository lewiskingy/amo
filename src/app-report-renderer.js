/* Pure Status Report renderer shared by the AMO workspace UI and the lightweight /reports viewer.
   It consumes only a report snapshot. Historical reports are scoped by projecting the persisted
   snapshot; the renderer never reads or recalculates from the live workspace. */
(function initAmoReportRenderer(){
  if(window.AmoReportRenderer)return;
  const ORG='organization',ALL='department';
  const esc=v=>String(v??'').replace(/[&<>"']/g,ch=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[ch]));
  const clone=v=>typeof structuredClone==='function'?structuredClone(v):JSON.parse(JSON.stringify(v));
  const normalizeHealth=v=>({Green:'On Track',Amber:'At Risk',Red:'Off Track','On Track':'On Track','At Risk':'At Risk','Off Track':'Off Track'}[String(v||'').trim()]||String(v||'').trim());
  const healthTone=v=>{const h=normalizeHealth(v);return h==='On Track'?'green':h==='At Risk'?'amber':h==='Off Track'?'red':'unset'};
  const moneyCompact=n=>new Intl.NumberFormat('en-GB',{style:'currency',currency:'GBP',notation:'compact',maximumFractionDigits:1}).format(Number(n)||0);
  const num=(n,d=0)=>Number(n||0).toFixed(d);
  const formatDate=v=>{if(!v)return'';const d=new Date(v);return Number.isNaN(d.getTime())?String(v):d.toLocaleString('en-GB')};
  const sectionTitle=(title,note='')=>`<div class="section-title report-section-title"><h2>${esc(title)}</h2>${note?`<span class="muted">${esc(note)}</span>`:''}</div>`;
  const kpi=(label,value,sub='')=>`<div class="card report-kpi"><div class="kpi-label">${esc(label)}</div><div class="kpi-value">${esc(value)}</div>${sub?`<div class="kpi-sub">${esc(sub)}</div>`:''}</div>`;
  const serviceText=entry=>Array.isArray(entry?.services)&&entry.services.length?entry.services.join(', '):String(entry?.service||'');

  function normalizeScope(scope={}){const departmentId=scope.departmentId||scope.departmentScopeId||ORG,teamId=departmentId===ORG?ALL:(scope.teamId||scope.teamScopeId||ALL);return{departmentId,teamId,departmentName:scope.departmentName||'',teamName:scope.teamName||'',label:scope.label||scope.scopeName||''}}
  function scopeLabel(scope){if(scope.label)return scope.label;if(scope.departmentId===ORG)return'Whole organisation';if(scope.teamId===ALL)return scope.departmentName||'Whole department';return[scope.departmentName,scope.teamName].filter(Boolean).join(' · ')||scope.teamName||scope.teamId}
  function entryDepartmentId(entry,teamsById={}){return entry?.departmentId||teamsById?.[entry?.teamId]?.departmentId||''}
  function projectReport(report,scopeInput={},catalog={}){if(!report)return report;const scope=normalizeScope(scopeInput),copy=clone(report),teamsById=catalog.teamsById||{};copy.entries=(report.entries||[]).filter(entry=>{if(scope.departmentId!==ORG&&entryDepartmentId(entry,teamsById)!==scope.departmentId)return false;return scope.teamId===ALL||entry.teamId===scope.teamId});const snapshots=report.dashboardSnapshots||{};let snapshot=report.dashboardSnapshot||null;if(scope.teamId!==ALL)snapshot=snapshots.teams?.[scope.teamId]||snapshot;else if(scope.departmentId!==ORG)snapshot=snapshots.departments?.[scope.departmentId]||(report.departmentScopeId===scope.departmentId?snapshots.department:null)||snapshot;else snapshot=snapshots.organization||snapshots.department||snapshot;copy.dashboardSnapshot=snapshot;copy.departmentScopeId=scope.departmentId;copy.teamScopeId=scope.teamId;copy.scopeName=scopeLabel(scope);return copy}

  function demandHighlights(s){if(!s)return'';const defs=[['Active demand',s.activeDemand,'Unresolved Architecture demand'],['Unallocated',s.unallocated,'Needs resource allocation'],['Capacity conflicts',s.capacityConflicts,'Actual/forecast person-period over-capacity']].filter(([,v])=>v!==undefined&&v!==null);return defs.length?`${sectionTitle('Portfolio position','Concise snapshot captured for this reporting cycle.')}<div class="grid kpis report-headline-kpis">${defs.map(([l,v,sub])=>kpi(l,String(v),sub)).join('')}</div>`:''}
  function capacityOutlook(s){const rows=s?.capacityOutlook||[];if(!rows.length)return'';const coverage=s?.actualsCoverage||'';return `${sectionTitle('Capacity outlook',coverage)}<div class="card report-snapshot-panel">${rows.map(r=>{const pct=Number(r.utilisationPct||0),used=Number(r.reportedFte??r.allocatedFte??0),basis=r.basis?` <span class="muted report-period-basis">${esc(String(r.basis).toUpperCase())}</span>`:'';return `<div class="capacity-row"><div class="flex"><strong>${esc(r.label||r.month||'')}${basis}</strong><span>${num(used,1)} / ${num(r.capacityFte,1)} FTE (${Math.round(pct)}%)</span></div><div class="bar ${pct>100?'bad':pct>85?'warn':'good'}"><span style="width:${Math.min(Math.max(pct,0),100)}%"></span></div></div>`}).join('')}</div>`}
  function healthSummary(entries){const totals={'On Track':0,'At Risk':0,'Off Track':0,Unset:0};entries.forEach(e=>{const h=normalizeHealth(e.health||e.rag);if(Object.prototype.hasOwnProperty.call(totals,h))totals[h]++;else totals.Unset++});return `<div class="report-health-summary" aria-label="Health summary"><div class="report-health-summary-title"><strong>Health overview</strong><span class="muted">${entries.length} reported item${entries.length===1?'':'s'}</span></div><div class="report-health-summary-items">${[['On Track','green'],['At Risk','amber'],['Off Track','red'],['Unset','unset']].map(([label,tone])=>`<div class="report-health-summary-item report-health-summary-${tone}"><span class="health-dot health-${tone}"></span><span>${label}</span><strong>${totals[label]}</strong></div>`).join('')}</div></div>`}
  function effortContext(context){if(!context)return'';const variance=Number(context.varianceToDateFte||0),signal=String(context.signal||'on-plan'),tone=signal==='over-plan'||signal==='under-plan'||signal==='no-actual'?'amber':signal==='unplanned'?'blue':'gray',label=signal.replaceAll('-',' ');return `<div class="report-effort-context"><div class="report-effort-context-head"><strong>Effort to date</strong><span class="pill ${tone}">${esc(label)}</span></div><div class="muted">Actual ${num(context.actualToDateFte,1)} FTE-mo · plan ${num(context.historicalForecastFte,1)} FTE-mo · variance ${variance>=0?'+':''}${num(variance,1)} FTE-mo</div>${context.message?`<div class="report-effort-message">${esc(context.message)}</div>`:''}</div>`}
  function workPackageContext(entry){const rows=Array.isArray(entry?.workPackages)?entry.workPackages:[];if(!rows.length)return'';return `<div class="report-work-packages"><div class="report-work-packages-title"><strong>Active Work Packages</strong><span class="muted">${rows.length}</span></div>${rows.map(w=>`<div class="report-work-package-row"><div><strong>${esc(w.id||'')}</strong>${w.title?` · ${esc(w.title)}`:''}</div><div class="report-work-package-meta">${w.service?`<span>${esc(w.service)}</span>`:''}${w.status?`<span class="pill blue">${esc(w.status)}</span>`:''}${w.targetEnd?`<span class="muted">Target ${esc(w.targetEnd)}</span>`:''}</div></div>`).join('')}</div>`}
  function narrative(report){const entries=report?.entries||[];return `${sectionTitle('Architecture Status Report','Demand-level Health, Work Package context, effort context and management narrative.')} ${healthSummary(entries)}<div class="report-narrative-list">${entries.length?entries.map(e=>{const health=normalizeHealth(e.health||e.rag),tone=healthTone(health),services=serviceText(e);return `<article class="report-entry report-health-${tone}" data-health="${esc(health||'Unset')}"><div class="report-entry-head report-health-head"><div><strong>${esc(e.title||e.demandId||'Demand')}</strong><div class="muted">${esc(e.demandId||'')}${e.owner?` · ${esc(e.owner)}`:''}${services?` · ${esc(services)}`:''}</div></div><div class="report-health-label" aria-label="Health: ${esc(health||'Unset')}"><span class="health-dot health-${tone}"></span><strong>${esc(health||'Health not set')}</strong></div></div>${workPackageContext(e)}${effortContext(e.effortContext)}<div class="report-entry-body"><div class="report-narrative"><h4>Status Update</h4><p>${esc(e.statusUpdate||'—')}</p></div><div class="report-narrative"><h4>Achievements</h4><p>${esc(e.achievements||'—')}</p></div><div class="report-narrative"><h4>Issues / Escalations</h4><p>${esc(e.issues||'—')}</p></div></div></article>`}).join(''):'<div class="notice">No narrative entries were captured for this scope.</div>'}</div>`}
  function renderReport(report,{showHeader=true,scope=null,catalog={}}={}){if(!report)return'<div class="notice">Report is unavailable.</div>';const projected=scope?projectReport(report,scope,catalog):report,status=projected.status||'Published',published=projected.publishedAt?` · Published ${formatDate(projected.publishedAt)}`:'',revision=projected.revision?` · Revision ${Number(projected.revision)}`:'',scopeText=projected.scopeName?` · ${projected.scopeName}`:'',isDraft=!['Published','Final'].includes(status),header=showHeader?`<header class="report-hero ${isDraft?'report-hero-draft':'report-hero-published'}"><div><div class="report-eyebrow">Architecture Management Office</div><h1>Architecture Status Report</h1><div class="report-meta">${esc(projected.reportingDate||'')}${esc(scopeText)}${esc(published)}${esc(revision)}</div></div><span class="pill ${isDraft?'amber':'green'}">${esc(status)}</span></header>`:'';const s=projected.dashboardSnapshot||null;return `<div class="report-card shared-report-renderer">${header}${demandHighlights(s)}${capacityOutlook(s)}${narrative(projected)}</div>`}
  window.AmoReportRenderer={renderReport,projectReport,normalizeScope,scopeLabel,normalizeHealth,healthTone,moneyCompact,ORG,ALL};
})();
