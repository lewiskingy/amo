{
  "schemaVersion": 3,
  "demandModelVersion": 2,
  "recordTypes": {
    "demand": {
      "idPrefix": "DEM-",
      "concept": "Defined Demand",
      "required": ["id", "title", "businessArea", "status"],
      "optional": ["initiative", "teamId", "priority", "ownerId", "initialEstimate", "projectNumber", "health", "context", "source", "funding", "fundingHistory", "legacy"],
      "creationSemantics": "Only Title and Business Area are user-mandatory at initial creation; Demand State is defaulted. Owning Team, priority, initial size, owner, Project Number, health and context may be added as the demand matures.",
      "initialEstimate": {
        "fields": ["size", "estimatedDays"],
        "sizes": ["XS", "S", "M", "L", "XL"],
        "semantics": "The configured T-shirt-size day value is snapshotted onto the Demand when selected. This preserves the early ROM baseline and is not replaced by Work Package estimates or resource forecast."
      },
      "projectNumberSemantics": "Optional Oracle Project Number used to reconcile Actuals and identify Project-backed/cross-charge Demand. Legacy Cost Centre / Project Code values are never promoted automatically.",
      "deliverySemantics": "Delivery service, status, dates, refined estimate and backlog linkage belong to child Work Packages. Detailed backlog description and acceptance criteria remain in the delivery backlog rather than being duplicated in AMO.",
      "legacySemantics": "Removed legacy Demand delivery metadata may be retained under legacy for audit/history during migration but is not used as current operational data."
    },
    "workPackage": {
      "idPrefix": "WP-",
      "required": ["id", "demandId", "title", "service", "status"],
      "optional": ["estimatedEffortDays", "targetStart", "targetEnd", "azureDevOpsWorkItemId", "modifiedAt"],
      "estimateSemantics": "estimatedEffortDays is the refined delivery estimate for the package. Reporting sums populated estimates and retains estimate coverage. Legacy Work Package romDays is read as estimatedEffortDays when encountered.",
      "serviceSemantics": "Architecture Service is classified per Work Package so one Defined Demand can contain different forms of Architecture work.",
      "backlogSemantics": "AMO stores enough metadata to identify and manage the Work Package but does not duplicate backlog description or acceptance criteria. Azure DevOps is the currently implemented work-item provider; provider-neutral Azure DevOps/Jira linkage is deferred.",
      "dateSemantics": "Demand delivery windows are derived from the earliest Work Package targetStart and latest Work Package targetEnd; dates are not authored on Demand."
    },
    "team": {
      "idPrefix": "USR-",
      "required": ["id", "teamId", "name", "roleId", "fte", "active"],
      "departmentSemantics": "A Person belongs to one Home Team; Department is derived from that Team."
    },
    "allocation": {
      "idPrefix": "ALLOC-",
      "required": ["id", "demandId", "teamMemberId", "forecast"],
      "semantics": "Allocation remains Person × Defined Demand × Month. forecast values are fractions of the Person's available FTE, so forecast FTE = allocation fraction × Person FTE."
    },
    "idea": {"idPrefix": "IDEA-", "required": ["id", "raisedBy", "raisedDate", "title", "description", "status"]},
    "statusReport": {
      "idPrefix": "SR-",
      "states": ["Draft", "Published", "Final"],
      "activeCycleFile": "status-reports/draft.json",
      "required": ["id", "status", "reportingDate", "entries"],
      "publishedRequired": ["publishedReportId", "publishedAt", "publishedBy"],
      "optional": ["previousReportId", "previousEntries", "revision", "modifiedAt", "modifiedBy", "finalizedAt", "finalizedBy", "unpublishedAt", "unpublishedBy"],
      "lifecycle": "Draft can be published and unpublished repeatedly. New Draft finalises the current Published report and starts the next Draft with previousReportId/previousEntries as its comparison baseline.",
      "draftConcurrency": "Optimistic three-way merge at status-report entry field level; conflicting edits to the same field require user reconciliation."
    }
  },
  "reportingInvariants": {
    "allocations": "Person × Defined Demand × Month; allocation fraction × Person FTE gives forecast FTE",
    "actuals": "Person × Project Number × Month reconciled through Demand.projectNumber",
    "reportedEffort": "Actuals for imported periods; Allocation Forecast elsewhere/future",
    "actualCost": "Imported Oracle actualCostGbp is authoritative and is never recalculated from AMO Role rates",
    "forecastCost": "Forecast days × Person Role day rate, falling back to reporting.defaultDayRate",
    "estimateCost": "Initial ROM and Work Package Estimate use reporting.defaultDayRate because named resource mix is not known",
    "projected": "Actual to date + Allocation Forecast for non-actualised periods",
    "workPackageActuals": "No Work Package Actuals are inferred from Project-level Actuals."
  },
  "workspace": {
    "legacyOptional": ["department"],
    "note": "workspace.department is retained only as a migration seed for pre-hierarchy workspaces."
  },
  "config": {
    "required": ["departments", "teams", "roles", "statuses", "workPackageStatuses", "services", "demandSizeDays"],
    "department": {"required": ["id", "name"]},
    "team": {"required": ["id", "name", "departmentId"]},
    "role": {"required": ["id", "name", "dayRate"]},
    "reporting": {
      "optional": ["standardHoursPerDay", "defaultDayRate"],
      "standardHoursPerDaySemantics": "Converts imported Actual hours into comparable effort days and monthly FTE.",
      "defaultDayRateSemantics": "Current blended estimate/fallback rate used for ROM/WP valuation and where a named Person's Role has no rate. This is distinct from retired legacy financialPlanning settings."
    },
    "statusesSemantics": "Portfolio lifecycle states for Defined Demand.",
    "workPackageStatusesSemantics": "Delivery states for Work Packages.",
    "servicesSemantics": "Architecture service classifications used by Work Packages.",
    "demandSizeDaysSemantics": "Default day values for XS/S/M/L/XL initial Demand sizing; selected values are snapshotted on Demand."
  }
}
